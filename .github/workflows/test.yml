name: Test

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]


jobs:
  test:
    strategy:
      matrix:
        # todo: include windows-latest, ubuntu-latest, macos-latest, ubuntu-22.04
        os:
          - macos-14
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Push
        uses: ./
        id: push
        with:
          path: test/files/gcd@*.wasm
          namespace: rocketniko
          name: gcd
          registry: wa.dev
        env:
          WARG_TOKEN: ${{ secrets.WARG_TOKEN }}
          WARG_PRIVATE_KEY: ${{ secrets.WARG_PRIVATE_KEY }}

      - name: Display outputs
        shell: bash
        run: |
          echo "filename:" ${{ steps.push.outputs.filename }}
          echo "digest:" ${{ steps.push.outputs.digest }}
          echo "namespace:" ${{ steps.push.outputs.namespace }}
          echo "name:" ${{ steps.push.outputs.name }}
          echo "version:" ${{ steps.push.outputs.version }}

          echo "registry:" ${{ steps.push.outputs.registry }}
          echo "registry type:" ${{ steps.push.outputs.registry-type }}
          echo "oci registry:" ${{ steps.push.outputs.oci-registry }}
          echo "oci namespace prefix:" ${{ steps.push.outputs.oci-namespace-prefix }}
          echo "warg url:" ${{ steps.push.outputs.warg-url }}
